# Captures

Store run-specific raw CDC captures and derived outputs here locally. Captures
are ignored by default because they can become large. Keep a small, intentional
fixture under `tests/fixtures` when a capture is needed for a regression test.
