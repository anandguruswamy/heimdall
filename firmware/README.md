# Heimdall Firmware

The initial contents are copied from the proven open Zephyr/DW3000 bring-up.
The next implementation step is to split its Phase 1 application into shared
radio behavior plus explicit `node` and `gateway` profiles.
