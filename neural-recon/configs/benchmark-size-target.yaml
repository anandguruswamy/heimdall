# Primary production candidate: paper-sized FFNs plus moderate depth reduction.
name: ab-size-d128-f512-e4-d3-g24
dataset_dir: datasets/stage1
epochs: 100
batch_size: 8
seed: 0
device: cuda
max_steps: 200
warmup_steps: 100
log_every: 100
eval_every: 100
early_stop_patience: 0
checkpoint_every: 1000
surfel_pulse_backend: bank-16x
capsule_attenuation_backend: compact
train_render_probability: 0.5
train_render_batch_size: 1
compact_surfel_slots: true
compile_model: true
compile_set_loss: true
model_d_model: 128
model_heads: 4
model_ffn: 512
model_encoder_blocks: 4
model_decoder_blocks: 3
model_queries: 24
